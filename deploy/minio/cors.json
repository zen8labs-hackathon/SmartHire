{
  "CORSRules": [
    {
      "AllowedOrigins": [
        "https://smart-hire.zen8labs.io",
        "https://smart-hire-dev.zen8labs.io",
        "http://localhost:3200",
        "http://localhost:3100",
        "http://localhost:3000"
      ],
      "AllowedMethods": ["GET", "PUT", "POST", "HEAD", "DELETE"],
      "AllowedHeaders": ["*"],
      "ExposeHeaders": ["ETag", "Content-Length", "Content-Type"],
      "MaxAgeSeconds": 3000
    }
  ]
}
